module.exports = {
  currency: "GBP",    // Default: "EUR" (loaded at run-time from the `coinmarketcap` api)
  gasPrice: 20 * 1e9  // Default: (~5 gwei, loaded at run-time from the `blockcypher` api)
}